#Create queue . Head is on the left, tail is on the right
queue = Queue()

#Size of the Queue
queue.size()

#Adding elements to a queue
queue.enqueue('element_to_add')

#Remove elements from the queue (Removes the first element)
queue.dequeue()

#See how the queue looks
queue.qlist
